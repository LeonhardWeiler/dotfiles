# KeePassXC-Passwortdatenbanken niemals versionieren
*.kdbx

# Editor-/Backup-Reste
*.bak

# Secrets/Runtime-State defensiv ausschliessen (analog zu *.kdbx) - falls solche
# Verzeichnisse/Dateien je unter config/ landen, gehoeren sie NICHT ins Repo.
# Siehe Health-Report TRACK-2 (Signal/ssh/pulse u. a.).
Signal/
pulse/
ssh/
*.pem
*.key
id_ed25519
id_rsa
